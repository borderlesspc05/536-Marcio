# OBSOLETO — Deploy Vercel + Prisma

Este guia descrevia bootstrap com Prisma/SQLite/Postgres.

**Runtime atual:** Firestore (`firestoreDb`) + Firebase Auth + JWT de sessão.  
**Deploy atual:** [`deploy-firebase.md`](./deploy-firebase.md) (Spark) + Vercel para o Next.

Não use `DATABASE_URL`, `prisma generate` ou `prisma db push` neste repositório.
